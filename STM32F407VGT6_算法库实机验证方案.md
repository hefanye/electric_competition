# STM32F407VGT6 算法库实机验证方案

## 1. 目标与边界

本方案的目标不是一次性完成整套仪器，而是把已完成的三类可移植算法库逐项搬到 **STM32F407VGT6** 上，用真实模拟信号验证：

- `filter`：滤波结果、瞬态响应、抗脉冲能力。
- `measure`：幅值、RMS、频率、相位、稳定度、校准等测量结果。
- `signal_process`：FFT、窗函数、频谱峰值、Goertzel、FSK、AM、NCO、扫频规划等。

本阶段只验证“ADC 采样数据进入算法后，输出是否符合已知输入”，不要求先做完整人机界面、自动量程或电源保护。

## 2. 现有设备的分工

| 设备 | 本次用途 | 不承担的职责 |
|---|---|---|
| STM32F407VGT6 | ADC 采样、DMA 缓冲、算法运行、串口打印结果 | 不直接产生高精度模拟测试信号 |
| AD9959 DDS | 产生正弦波、双频 FSK、AM/扫频等交流测试信号 | 不能直接接 ADC，必须先处理幅度和偏置 |
| 1.65 V 直流偏置模块 | 将双极性交流信号平移至 ADC 可采范围 | 不提供交流信号 |
| DAC8830 双路 DAC | 产生已知直流电平、慢变化阶跃/斜坡，验证直流测量和滤波瞬态 | 不适合直接产生高质量高频波形 |
| 示波器 | 检查 ADC 输入波形是否削顶、是否带偏置、确认实际频率和幅度 | 不能替代算法结果记录 |
| 串口助手/串口屏 | 输出测试项目、参数、算法结果和 PASS/FAIL | 不参与高频采样链路 |

## 3. 首先搭建统一测试链路

所有后续测试都基于同一条链路。先搭好并确认，后面不反复改硬件。

```text
AD9959 / DAC8830
        │
        ├── 限幅、衰减或运放调理 ──┐
        │                          ├── ADC 输入（例如 ADC1 某一路）
1.65 V 偏置 ────────────────────────┘
                                           │
                                   ADC + DMA 双缓冲
                                           │
                    原始码值 -> 电压值 -> 算法库 -> UART 输出结果
```

### 3.1 ADC 输入的硬性限制

F407 的 ADC 引脚允许范围是 **0～3.3 V**。接入前先用示波器确认：

- 波形最低点必须高于约 `0.1 V`。
- 波形最高点必须低于约 `3.2 V`。
- 推荐初始设置：偏置 `1.65 V`，交流峰值不超过 `1.0 V`，即大约 `0.65～2.65 V`。
- DDS 输出、偏置模块、STM32、示波器必须共地。
- 若 DDS 输出是 5 Vpp 或更大，必须先衰减；禁止直接接到 ADC。

### 3.2 两路同步采样的预留

相位差、阻抗和相关测量需要两路 ADC 同步采样。即使第一阶段只接一路，也建议在 PCB/杜邦线层面预留：

- ADC1 通道 A：待测信号。
- ADC2 或 ADC1 第二规则通道 B：参考信号。
- 两路使用同一个定时器触发，DMA 存为交错数组，例如 `A0,B0,A1,B1...`。

不要用两个独立软件触发的 ADC 来测相位；采样时刻不确定会直接破坏相位结果。

## 4. 固件总体结构

推荐采用“定时器触发 ADC，DMA 搬运，主循环处理整块数据”的结构：

```text
TIM6/TRGO
   ↓
ADC 规则组转换
   ↓
DMA 循环/双缓冲写入 sample_buffer
   ↓（半传输或全传输标志）
主循环处理一块完整数据
   ├── 原始 ADC 码值转换为电压
   ├── 去直流/滤波
   ├── 测量或频谱算法
   └── UART 输出结果与 PASS/FAIL
```

原则：

- ADC、DMA、定时器负责“稳定取样”。
- 中断中只置半缓冲/满缓冲标志，不能在中断中运行 FFT、Goertzel 或大量 `printf`。
- 算法在主循环中处理完整数据块。
- 每个测试项目单独启用，不要一次运行所有算法。

## 5. 统一采样参数建议

第一版不追求覆盖所有频段，先建立一套稳定基准。

| 测试类别 | 推荐采样率 `fs` | 数据长度 `N` | 适用说明 |
|---|---:|---:|---|
| 直流、慢变化、滤波 | 1 kHz～10 kHz | 128～512 | 方便观察阶跃和噪声 |
| 50/100/500 Hz 测量 | 10 kHz | 1000 或 2000 | 整周期采样，便于 RMS/频率对照 |
| FFT 基础验证 | 8192 Hz 或 16384 Hz | 256/512/1024 | 频点可整齐落入 FFT bin |
| Goertzel/FSK | 8 kHz～20 kHz | 依据目标频率取整周期 | 降低频谱泄漏 |
| 高频上限测试 | 按 ADC 调理带宽确定 | 256/512 | 必须保证 `fs ≥ 5 × f信号`，不要只卡奈奎斯特两倍 |

优先使用“整数周期采样”：例如 `fs=8192 Hz，N=256` 时，频率分辨率为 `32 Hz`；可先使用 `32、64、128、256、512 Hz` 等频率做基础测试。

## 6. 验证顺序

必须按下列顺序进行。前一层没有通过，不能用后一层的异常来判断算法错误。

### 阶段 0：ADC 基线与量程确认（先做）

1. ADC 输入接 1.65 V 偏置，不接交流信号。
2. 连续采集 1000 点，输出平均值、最小值、最大值、峰峰值、标准差。
3. 预期：平均 ADC 电压接近 1.65 V；无削顶；噪声范围稳定。
4. 记录该数据，作为后续信号质量与 DC 阻断测试的基线。

再分别输入约 `0.5 V、1.0 V、1.65 V、2.3 V、2.8 V` 的稳定直流电平（可由 DAC8830 经正确参考和调理获得），建立 ADC 码值到电压的校准表。

通过标准：误差符合当前 ADC 参考源、分压和运放精度的预期，且不存在靠近 0 V/3.3 V 的削顶。

### 阶段 1：滤波库验证

#### 1. 移动平均

- 输入：DAC8830 产生阶跃直流；或 DDS 加小噪声后经偏置输入。
- 测试：窗口 `N=4、8、16、32`。
- 观察：稳态噪声下降；阶跃响应延迟约为 `(N-1)/2` 个采样点。
- 判定：输出平均值正确，不出现缓冲区越界、启动阶段异常或长期漂移。

#### 2. 中值滤波

- 输入：稳定直流或低频正弦；人为用跳线/继电器制造偶发尖峰，或在测试数组中注入尖峰。
- 测试：窗口 `3、5`。
- 判定：单点尖峰基本被剔除，正常阶跃不会被长期抹平。

#### 3. 一阶低通

- 输入：低频正弦叠加高频噪声，或 DAC 阶跃。
- 测试：改变 `alpha`，记录上升时间和不同频率下的幅值。
- 判定：`alpha` 小时更平滑但响应更慢；截止附近幅值趋势符合预期。

#### 4. DC 阻断

- 输入：`1.65 V 偏置 + 正弦`。
- 判定：输出均值逐步接近零；50 Hz/100 Hz 正弦幅值不能明显衰减。
- 注意：DC 阻断后数据可能为负数，只能放在 `float` 或有符号数据链路中，不能再按 ADC 无符号码值处理。

#### 5. Biquad（二阶通用滤波器）

- 输入：DDS 依次产生低于、等于、高于截止频率的正弦。
- 验证低通、高通、带通、陷波四种系数配置。
- 对于 50 Hz 陷波：测试 50 Hz 与 100 Hz 的幅值比，确认 50 Hz 被明显压低而 100 Hz 保留。
- 判定：无自激、无溢出、切换系数后状态可正确复位。

### 阶段 2：测量库验证

测量模块在滤波链路稳定后验证。每个项目都需记录“DDS/示波器参考值、算法值、绝对误差、相对误差”。

| 模块 | 输入与方法 | 通过判定 |
|---|---|---|
| 平均值/极值/峰峰值 | 已知直流、已知幅度正弦 | 与示波器/理论值一致，单位和偏置处理正确 |
| RMS/AC RMS | 1 kHz 或 100 Hz 正弦；先去 DC 再算 AC RMS | 峰值为 `A` 的正弦，RMS 接近 `A/√2` |
| 波峰因数 | 正弦、方波、削顶波 | 正弦约 1.414；削顶后波峰因数下降 |
| 过零测频 | 50、100、500、1000 Hz；带 1.65 V 偏置时先去偏置或使用阈值 | 多周期平均后频率稳定，低频优先于 FFT 测频 |
| 相位/IQ | 两路同频正弦，DDS 设定已知相位差 | 0°、30°、90°、180° 都能区分；注意相位正负定义 |
| 信号质量 | 正常正弦、故意削顶、无信号 | 能标出削顶、过小幅度、有效/无效信号 |
| 稳定度 | 稳定直流与稳定正弦；再制造突变 | 稳定窗口触发正确，异常点使稳定判定失效 |
| 两点/分段校准 | 用 2～5 个已知直流点 | 校准后读数相对未校准明显改善 |

频率测量的实际优先级：先使用“过零 + 插值 + 多周期平均”；只有需要同时得到谐波或频谱时，再使用 FFT。

### 阶段 3：信号处理库验证

#### 1. 窗函数、FFT 与频谱峰值

1. 先输入恰好落在 FFT bin 的单频正弦，验证峰值 bin 和频率。
2. 再输入不落 bin 的频率，分别比较矩形窗、Hann 窗、Hamming 窗的泄漏。
3. 检查窗函数的相干增益补偿后，幅值是否仍接近实际值。
4. 用两音信号验证主峰、次峰和动态范围。

通过标准：频率峰值正确；窗函数开启后旁瓣下降；幅值换算没有整体差一倍或差 `N` 倍。

#### 2. Goertzel

- 输入目标频率和非目标频率各一组。
- 用于测某个固定频率的幅值、DTMF/FSK 判决。
- 判定：目标频率的能量明显高于非目标频率；频率偏移到容差外后能量下降符合预期。

#### 3. FSK 判决

- AD9959 在 `f0` 与 `f1` 之间切换。
- 每个符号周期采集一个完整数据块，分别计算两个 Goertzel 能量。
- 判定：`E(f0)>E(f1)` 时识别为 0，反之为 1；在低幅度和频偏条件下记录误码边界。

#### 4. AM 包络

- DDS 产生带已知调制度的 AM 信号，或使用外部调制源。
- 先验证包络最大值、最小值，再计算调制度：`m=(Vmax-Vmin)/(Vmax+Vmin)`。
- 判定：调制度趋势和设置一致；载波幅度变化时不误判为调制度变化。

#### 5. NCO

- 不必先接 DAC 输出；先在内存中产生 NCO 序列并用 FFT 检查频率和相位连续性。
- 需要后续输出模拟波时，再将 NCO 查表数据送到 DAC/DMA 链路。

#### 6. 扫频规划器

- 不接真实被测网络也可先验证线性扫频和对数扫频的频点序列。
- 将每个频点打印出来，与开始、结束、步进/点数配置比对。
- 后续可与 DDS 命令库配合：设置频率 → 等待稳定 → 采样测量 → 保存结果 → 下一个频点。

## 7. 每个项目的统一测试模板

每次只验证一个模块，在 `main.c` 中临时启用对应测试函数。每个测试至少打印：

```text
TEST: RMS_100HZ
fs=10000 Hz, N=1000
reference=0.7071 V
actual=0.7048 V
abs_error=0.0023 V
rel_error=0.33 %
RESULT=PASS
```

建议每项测试同时保留：

1. 固定参数测试：便于回归验证。
2. 边界参数测试：最小幅度、接近满量程、最低/最高频率。
3. 异常输入测试：削顶、无信号、尖峰、频率偏移、DDS 未返回稳定信号。

## 8. 建议的 PASS 标准

不要一开始要求极高精度。算法正确性与硬件精度要分开判断。

| 项目 | 第一阶段建议目标 |
|---|---|
| 均值/直流测量 | 校准后误差 ≤ 1 % 或由参考源精度决定 |
| 正弦 RMS | 误差 ≤ 2 % |
| 峰峰值 | 误差 ≤ 2 %，且无削顶 |
| 过零测频 | 100 Hz 及以上误差 ≤ 0.5 %；多周期平均后更好 |
| 相位 | 初版允许 ±3°～5°，先保证符号和象限正确 |
| FFT 主频 | 误差不大于一个 bin；使用峰值插值后再提高 |
| Goertzel/FSK | 目标频率能量至少明显高于另一频率；记录最小可判别信噪比 |
| 滤波 | 趋势、响应时间、截止/陷波方向正确，不溢出不发散 |

## 9. 常见失败原因与排查顺序

1. **先看示波器**：ADC 引脚是否削顶、是否真有 1.65 V 偏置、是否共地。
2. **再看原始 ADC 数据**：均值、最小值、最大值是否合理；不要直接怀疑 FFT。
3. **确认采样率**：定时器 TRGO 实际频率是否等于软件里传入算法的 `fs`。
4. **确认数据块完整**：DMA 半缓冲和满缓冲是否重复处理或漏处理。
5. **确认去偏置位置**：DC 阻断、减均值应发生在 AC RMS、过零、FFT 等算法之前。
6. **确认单位**：DDS 设置常为 Hz，ADC 为码值，算法可能使用 V；不要混用。
7. **确认整周期**：FFT/Goertzel 初测尽量让采样窗口包含整数周期。
8. **最后才调算法参数**：窗口长度、`alpha`、Biquad 系数、阈值等。

## 10. 推荐实际执行清单

按以下次序逐项完成并记录结果：

1. 1.65 V 静态 ADC 基线。
2. DAC8830 多点直流输入与 ADC 校准。
3. 移动平均、中值、一阶低通、DC 阻断。
4. 正弦峰值、峰峰值、RMS、AC RMS、波峰因数。
5. 过零测频与稳定度。
6. 二阶 Biquad 低通/陷波。
7. FFT + 窗函数 + 峰值检测。
8. Goertzel。
9. FSK 判决。
10. 双通道同步采样后的相位/IQ 测量。
11. AM 包络、扫频与后续阻抗/传递函数项目。

完成某项后再把该项从临时 `main.c` 测试代码整理为独立函数或测试文件；不要在尚未确认硬件链路时一次性把所有模块都接入。
